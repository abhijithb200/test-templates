id: status-200-check

info:
  name: HTTP 200 OK Check
  author: you
  severity: info

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: status
        status:
          - 200
