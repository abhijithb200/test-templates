id: status-404-check

info:
  name: HTTP 404 Not Found Check
  author: you
  severity: info

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: status
        status:
          - 404
